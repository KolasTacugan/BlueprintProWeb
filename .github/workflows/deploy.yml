name: Build and deploy ASP.NET Core app to Azure Web App

env:
  AZURE_WEBAPP_NAME: BlueprintPro
  AZURE_WEBAPP_PACKAGE_PATH: './BlueprintProWeb/publish'
  DOTNET_VERSION: '8.0.x'

on:
  push:
    branches: [ "master" ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1. Pull the repo
      - uses: actions/checkout@v4

      # 2. Install .NET SDK
      - name: Setup .NET Core
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      # 3. Install EF Core CLI
      - name: Install dotnet-ef tool
        run: dotnet tool install --global dotnet-ef

      # 4. Add EF tool to PATH
      - name: Add .NET tools to PATH
        run: echo "$HOME/.dotnet/tools" >> $GITHUB_PATH

      # 5. Restore dependencies
      - name: Restore dependencies
        run: dotnet restore
        working-directory: ./BlueprintProWeb

      # 6. Build
      - name: Build with dotnet
        run: dotnet build --configuration Release --no-restore
        working-directory: ./BlueprintProWeb

      # 7. Run EF Core migrations
      - name: Apply EF Core Migrations
        run: dotnet ef database update --project ./BlueprintProWeb
        env:
          ConnectionStrings__DefaultConnection: ${{ secrets.AZURE_SQL_CONNECTION_STRING }}

      # 8. Publish app
      - name: Publish app
        run: dotnet publish -c Release -o ./publish
        working-directory: ./BlueprintProWeb

      # 9. Upload build artifact
      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: .net-app
          path: ./BlueprintProWeb/publish

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'Development'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: .net-app

      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v2
        with:
          app-name: ${{ env.AZURE_WEBAPP_NAME }}
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: ./BlueprintProWeb/publish
        env:
          ConnectionStrings__DefaultConnection: ${{ secrets.AZURE_SQL_CONNECTION_STRING }}
          OpenAI__ApiKey: ${{ secrets.OPENAI_API_KEY }}
          Stripe__PublishableKey: ${{ secrets.STRIPE_PUBLISHABLE_KEY }}
          Stripe__SecretKey: ${{ secrets.STRIPE_SECRET_KEY }}
